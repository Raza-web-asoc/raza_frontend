services:
  frontend:
    build: .
    container_name: raza-frontend
    ports:
      - "80:80"
    env_file:
      - .env
    volumes:
      - .:/app
    networks:
      - autenticador_network

networks:
  autenticador_network:
    external: true